#!/usr/bin/env bash
# Build Path: /app/.heroku/php/
# Build Deps: extensions/no-debug-non-zts-20121212/amqp-bare-1.7.1, libraries/librabbitmq-0.8.0

# That's it - we're combining the dep build and the extension build here

source $(dirname $0)/amqp
